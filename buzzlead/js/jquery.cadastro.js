function exibirDados(){	var $form = $('#name, #cnpj, #address, #state, #city, #creationDate')	var obj = {}	var count = 0;		$form.each(function(){		if($(this).val() == ''){			var $this = $(this);						$(function(){    			$this.popover('show'); 			});			setTimeout(function () {        		$this.popover('hide');    		}, 3000);			return false;			}		else if(($(this).attr('id') == 'creationDate') && ($(this).val() < 10)){			var $this = $(this);						$(function(){    			$this.popover('show'); 			});			setTimeout(function () {        		$this.popover('hide');    		}, 3000);			return false;		}		else if(($(this).attr('id') == 'cnpj') && ($(this).val() < 18)){			var $this = $(this);						$(function(){    			$this.popover('show'); 			});			setTimeout(function () {        		$this.popover('hide');    		}, 3000);			return false;		}		else{			obj[this.id] = $(this).val();			count++		}	});			var valuesJSON = JSON.stringify(obj);		if((valuesJSON.length != 2) && (count == 6)){		console.log(valuesJSON);		console.log(valuesJSON.length);		alert(valuesJSON);	}	else{		console.log("Erro de entrada de dados!")	}}(jQuery);